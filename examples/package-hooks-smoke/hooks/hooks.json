{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const fs=require('fs');const path=require('path');const root=process.env.CLAUDE_PROJECT_DIR||process.cwd();const dir=path.join(root,'.codemini');fs.mkdirSync(dir,{recursive:true});const marker=path.join(dir,'package-hooks-smoke.marker');fs.writeFileSync(marker,new Date().toISOString()+'\\n');console.log(JSON.stringify({hookSpecificOutput:{additionalContext:'[package-hooks-smoke] SessionStart OK — marker at .codemini/package-hooks-smoke.marker'}}));\""
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"console.log(JSON.stringify({hookSpecificOutput:{additionalContext:'[package-hooks-smoke] UserPromptSubmit OK'}}));\""
          }
        ]
      }
    ]
  }
}
